generator client {
  provider        = "prisma-client-js"
  output          = "./generated/client"
  previewFeatures = ["fullTextSearch"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model admin {
  id                     Int       @id @default(autoincrement())
  btoa                   String?   @unique
  start                  DateTime  @default(now())
  end                    DateTime? @updatedAt
  nama_barang            String?
  kategori_barang        String?
  tag_barang             String?
  harga_barang           Int?
  diskon_barang          Int?
  rating_barang          Int?
  jumlah_barang          Int?
  total_penjualan_barang Int?
  diskripsi_barang       String?
  gambar_barang          String?
  slug_barang            String?   @unique
  kupon_barang           String?
  view_barang            Int?
  like_barang            Int?
  link_barang            String?
}

model formPembelian {
  id                  Int       @id @default(autoincrement())
  start               DateTime  @default(now())
  end                 DateTime? @updatedAt
  nota_user           String?   @unique
  nama_lengkap_user   String?
  alamat_lengkap_user String?
  kode_pos_user       Int?
  no_hp_user          Int?
  catatan_user        String?
  status_pesanan      String?
  dataPesanan         Json?
  payment             Boolean   @default(false) 
}
